name: Keep Backend Service Active

on:
  schedule:
    - cron: '*/1 * * * *'
  workflow_dispatch:

jobs:
  health_check:
    runs-on: ubuntu-latest
    steps:
      - name: Ping frontend
        run: curl -f ${{ secrets.GATEWAY_BACKEND_URL }}/health