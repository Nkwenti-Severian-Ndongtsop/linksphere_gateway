name: Keep Frontend Service Active

on:
  schedule:
    - cron: '*/3 * * * *'
  workflow_dispatch:

jobs:
  health_check:
    runs-on: ubuntu-latest
    steps:
      - name: Ping frontend
        run: curl -f ${{ secrets.GATEWAY_FRONTEND_URL }} 
